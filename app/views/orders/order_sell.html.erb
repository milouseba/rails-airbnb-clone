<h1>Liste de mes ventes</h1>

<ul>
  <% @orders.each do |order| %>
  <li>
    <%= order.quantity %>
    <%= order.article.title %>
    <%= order.confirmed %>
    <%= order.delivered %>
    <%= link_to "Confirmed", orders_sell_confirmed_path(order), method: :patch %>
  </li>
  <% end %>
</ul>
